# Module 02 — Validation: JavaScript Basics

Answer each question in your own words.

If you cannot answer a question, go back and review that section.
Do not move to Module 03 until most of these are comfortable.

---

## Section 1 — Variables

**1.** What is the difference between `let` and `const`?

> Write your answer here.

---

**2.** What is wrong with this code? Fix it.

```js
const score = 10;
score = 20;
```

> Write your answer here.

---

**3.** What is the difference between `null` and `undefined`?

> Write your answer here.

---

## Section 2 — Arrays and objects

**4.** What does this code print? Why?

```js
const fruits = ['apple', 'banana', 'cherry'];
console.log(fruits[1]);
```

> Write your answer here.

---

**5.** What is the difference between `map()` and `filter()`?

> Write your answer here.

---

**6.** Look at this object:

```js
const car = { brand: 'Toyota', year: 2020, electric: false };
```

- How do you get the `brand` value?
- How do you change `electric` to `true`?
- How do you add a new property `color: 'red'`?

> Write your answer here.

---

## Section 3 — Functions

**7.** What is the difference between a parameter and an argument?

> Write your answer here.

---

**8.** What does this function return? Explain why.

```js
function multiply(a, b) {
  return a * b;
}

console.log(multiply(3, 4));
```

> Write your answer here.

---

**9.** Rewrite this function as an arrow function:

```js
function double(n) {
  return n * 2;
}
```

> Write your answer here.

---

## Section 4 — Conditions and loops

**10.** What is the difference between `==` and `===`? Why should you use `===`?

> Write your answer here.

---

**11.** What does this code print?

```js
const numbers = [1, 2, 3, 4, 5];
const result = numbers.filter(n => n % 2 === 0);
console.log(result);
```

> Write your answer here.

---

## Section 5 — Can you do it?

- [ ] I can declare variables with `let` and `const`
- [ ] I can create arrays and access items by index
- [ ] I can use `map`, `filter`, and `find` on arrays
- [ ] I can create objects and access their properties
- [ ] I can write a function with parameters and a return value
- [ ] I can write an arrow function
- [ ] I can use `if/else` for conditions
- [ ] I can loop through an array with `for...of`
- [ ] I can explain what JSON is

---

## You are ready for Module 03 when:

All boxes above are checked and you can answer at least 9 of the 11 questions without looking at notes.

**Next step:** Go to `03-javascript-dom/theory.md`
